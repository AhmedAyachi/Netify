/*const routes=[homeRoute];
    let currentindex=0;
    const history=window.history;
    const length=history.length;
    history.push=(route,state={})=>{
        if(route){
            const app=document.getElementById("app");
            app.innerHTML="";
            currentindex=routes.length;
            routes.push(route);
            if(currentindex>length){
                routes.shift();
                currentindex--;
            }
            route({parent:app,state});
        }
    }
    history.goBack=()=>{
        if(currentindex>0){
            history.push(routes[currentindex-1]);
            currentindex--;
        }
    }
    history.goForward=()=>{
        if(currentindex<routes.length){
            push(routes[currentindex+1]);
            currentindex++;
        }
    }
    history.replace=(route,state={})=>{
        const app=document.getElementById("app");
        app.innerHTML="";
        routes[currentindex]=route;
        route({parent:app,state});
    }
    history.goTo=(index=0)=>{
        if(-1<index&&index<length){
            const app=document.getElementById("app");
            routes[index]({parent:app});
        }
    };*/